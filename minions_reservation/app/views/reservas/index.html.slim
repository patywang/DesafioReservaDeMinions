h1 Listing reservas

table
  thead
    tr
      th Descricao
      th Minions
      th Data
      th
      th
      th

  tbody
    - @reservas.each do |reserva|
      tr
        td = reserva.descricao
        td = reserva.minions
        td = reserva.data
        td = link_to 'Show', reserva
        td = link_to 'Edit', edit_reserva_path(reserva)
        td = link_to 'Destroy', reserva, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New Reserva', new_reserva_path
